extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/subscribe") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/logout") Logout
    
